// Common Ping Service (prototype)

// Avoid Serializing Anything

include "../Standards/DateTimeOffset.fbs";
include "../Standards/GUID.fbs";

table Ping {
	ServerGuid: FbsGUID;
	KudoGuid: FbsGUID;
	Now: FbsDateTimeOffset;
}

table PingArg {
	dummy: int;
}

rpc_service Ping {
  Get(PingArg):Ping;    // I can't do a void here?
}